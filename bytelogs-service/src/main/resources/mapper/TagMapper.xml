<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.harrylei.forum.service.article.repository.mapper.TagMapper">

    <resultMap id="TagSimpleVOMap" type="top.harrylei.forum.api.model.vo.article.vo.TagSimpleVO">
        <id property="tagId" column="id"/>
        <result property="tagName" column="tag_name"/>
        <result property="tagType" column="tag_type"/>
    </resultMap>

    <select id="listSimpleTagsByIds" resultMap="TagSimpleVOMap">
        select id, tag_name, tag_type
        from tag
        <where>
            <choose>
                <when test="ids != null and ids.size() > 0">
                    id in
                    <foreach collection="ids" item="id" open="(" separator="," close=")">
                        #{id}
                    </foreach>
                    and deleted = 0
                </when>
                <otherwise>
                    0 = 1
                </otherwise>
            </choose>
        </where>
    </select>

</mapper>